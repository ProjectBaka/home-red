<p style="color: green"><%= notice %></p>

<h1>Locations</h1>
<%= datagrid_form_for @grid, :method => :get, :url => locations_path %>
<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<%= link_to "New location", new_location_path %>
